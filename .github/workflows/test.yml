name: "Test the action"
on:
  push:
    branches: [ main ]

jobs:
  
  Test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: ./
      with:
        AI_PATH: ./AssemblyInfo.cs
